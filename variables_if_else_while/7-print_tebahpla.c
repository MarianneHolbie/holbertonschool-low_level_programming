#include <stdio.h>
/**
 * main- Entry point
 * Description: reverse alphabet
 * Return: 0
*/
int main(void)
{
	int ascii;

	/* reverse loop to print reverse alphabet */
	ascii = 122;
		while (ascii == 96)
		{
			putchar(ascii);
			ascii--;
		}

	putchar('\n');
	return (0);
}
